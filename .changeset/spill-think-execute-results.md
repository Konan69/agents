---
"@cloudflare/think": patch
---

Spill oversized execute results to the configured workspace and return a bounded preview with a durable retrieval path instead of silently truncating the full value.
